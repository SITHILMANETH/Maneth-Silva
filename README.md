# Maneth Silva Portfolio

Modern one-page portfolio website for a robotics and artificial intelligence undergraduate student.

## Folder Structure

```text
Personal Profolio/
|-- assets/
|   `-- images/
|       |-- project-drone.svg
|       |-- project-swarm.svg
|       `-- project-vision.svg
|-- index.html
|-- style.css
|-- script.js
`-- README.md
```

## Run In Visual Studio Code

1. Open the folder in Visual Studio Code.
2. Install the `Live Server` extension if it is not already installed.
3. Right-click `index.html`.
4. Select `Open with Live Server`.
5. The portfolio will open in your browser and reload automatically when files change.

If you do not want to use an extension, you can also open `index.html` directly in a browser, but Live Server gives a better development workflow.

## Deploy With GitHub Pages

1. Create a new GitHub repository and push this folder to it.
2. Make sure `index.html` is in the root of the repository.
3. Open the repository on GitHub.
4. Go to `Settings` -> `Pages`.
5. Under `Build and deployment`, choose `Deploy from a branch`.
6. Select the `main` branch and the `/ (root)` folder.
7. Save the settings and wait for GitHub Pages to publish the site.
8. Your portfolio will be available at a URL similar to:

```text
https://your-username.github.io/your-repository-name/
```

## Notes

- Replace the placeholder GitHub, LinkedIn, email, and demo links with your real links.
- Update the project descriptions and skill levels with your real work before publishing.
